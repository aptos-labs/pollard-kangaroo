[package]
name = "pollard-kangaroo"
version = "0.1.0"
edition = "2021"

[features]
default = ["bl12", "bsgs", "bsgs_k", "bl12_table16", "bl12_table32", "bl12_table48", "bsgs_table32", "bsgs_k_table32"]
serde = ["dep:serde", "dep:serde_with"]
presets = ["serde", "dep:bincode"]
# Algorithm features - can be disabled to reduce WASM size
bl12 = []
bsgs = []
bsgs_k = []
# [BL12] precomputed table features
bl12_presets = ["bl12", "presets"]
bl12_table16 = ["bl12_presets"]
bl12_table32 = ["bl12_presets"]
bl12_table48 = ["bl12_presets"]
# BSGS precomputed table features
bsgs_presets = ["bsgs", "serde", "dep:bincode"]
bsgs_table32 = ["bsgs_presets"]
# BSGS-k precomputed table features
bsgs_k_presets = ["bsgs_k", "serde", "dep:bincode"]
bsgs_k_table32 = ["bsgs_k_presets"]

[dependencies]
curve25519-dalek = { version = "4", features = ["serde", "rand_core"] }
anyhow = { version = "1", default-features = false }
rand_core = { version = "0.6", default-features = false, features = ["getrandom"] }
serde = { version = "1", features = ["derive"], optional = true }
serde_with = { version = "3.12", optional = true }
bincode = { version = "1.3.3", optional = true }
web-time = { version = "1.1" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "dlp_algorithms"
harness = false
